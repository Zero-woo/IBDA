spring:
  datasource:
    url: jdbc:mysql://0.0.0.0:0000/IBDA
    username: hi
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.sql: debug